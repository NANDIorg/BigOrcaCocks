{
  "name": "orca-board",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter @orca-board/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test"
  }
}
