# Ревью task_mucv52ux1k

Вердикт: REJECT. Проверен коммит da9dfe3 ветки orca/task_mucv52ux1k относительно master.

## Требуемое исправление

P1 — apps/desktop/src/renderer/src/App.tsx:584: условие `tab === 'board' && openGlobal` оставляет в шапке кнопку «Новая задача» при переходе из глобальной задачи на вкладку «Терминалы» (также «О проекте»). Обработчик вызывает `setGlobalModal({ mode: 'create' })`, хотя контекст `openGlobal` сохраняется. Сохранение этой модалки вызывает `globalTasks.create`, то есть создаёт другую глобальную задачу. Это нарушает заданный критерий отсутствия случайного создания новой задачи из шапки, включая вкладку терминалов.

Воспроизведение: открыть существующую глобальную задачу → «Терминалы» → «Новая задача». Открывается создание глобальной задачи вместо действия в текущем контексте.

Исправление: выбирать действие создания по `openGlobal` независимо от вкладки (например, «Новая подзадача» с `setShowNew(true)`), либо скрывать создание на вспомогательных вкладках открытой глобальной задачи. На общей доске сохранить создание глобальных задач. Проверить также «О проекте» и «Входящие».

## Проверки

- `orca-board review info --task task_mucv52ux1k`: коммит da9dfe3, рабочая ветка разработчика чистая.
- `git diff master...orca/task_mucv52ux1k`: один изменённый файл, 13 добавлений и 3 удаления.
- `git merge --no-commit --no-ff orca/task_mucv52ux1k`, затем `pnpm typecheck`: успешно для core, cli, desktop. Временное объединение отменено через `git merge --abort`.
- `node review/task_mucv52ux1k.cjs` на временно объединённой ветке: исполнен исходный JSX шапки для board/terminals/info с открытой глобальной задачей и без неё. Воспроизведено создание глобальной задачи из контекста текущей на terminals/info. Скрипт фиксирует наблюдаемое поведение, включая дефект, и не является проверкой отсутствия регрессий.
- Исполнен исходный `startGlobalCoordinator` с mock IPC: inbox — ничего не вызывает; живой координатор — открывает существующий PTY без confirm/запуска; обычный запуск — передаёт текущий g.id; отмена — не запускает.
- По коду GlobalTaskView: координатор текущей задачи открывается через её coordinatorPty; для Входящих действие скрыто. GlobalBoard также скрывает запуск для Входящих. Создание через координатора на общей доске сохранено; в открытой глобальной задаче кнопка скрыта независимо от вкладки.
- Интерактивный Electron не проверялся, приложение не перезапускалось.

Для повторения скрипта нужна проверяемая версия App.tsx (временный merge), а также установленные зависимости desktop.
