import type { WfNodeType, WfOutcome } from '@orca-board/core'

// Справка по типам нод воркфлоу для редактора: палитра холста, инспектор выбранной ноды и легенда «Типы нод».
// Тексты — пересказ поведения движка (`packages/core/src/workflow.ts`, эффекты — `apps/desktop/src/main/workflow.ts`)
// и таблицы «Этапы и что делает приложение» в docs/workflow.md: меняется поведение этапа — правь и здесь.

export interface WfNodeHelp {
  /** Одна фраза: зачем этап. Идёт в подсказку кнопки палитры. */
  summary: string
  /** Кто выполняет этап. */
  actor: string
  /** Что происходит, пока задача на этапе. */
  details: string
  /** Смысл каждого исхода (порта). Набор ключей совпадает с `WF_PORTS` типа — это проверяет тест. */
  outcomes: Partial<Record<WfOutcome, string>>
  /** Какие поля настраиваются в инспекторе и на что влияют. */
  fields: string[]
}

export const WF_NODE_HELP: Record<WfNodeType, WfNodeHelp> = {
  start: {
    summary: 'Точка входа: с неё задача входит в граф при первом запуске воркера.',
    actor: 'Приложение — проходит этап сразу.',
    details: 'Старт в графе ровно один, и в него не может вести переход. Задача на нём не стоит: сразу уходит по «дальше».',
    outcomes: { next: 'первый этап, обычно «Работа».' },
    fields: ['Название — только для отображения.']
  },
  work: {
    summary: 'Воркер-агент делает задачу в своей ветке и сдаёт её командой done.',
    actor: 'Агент роли: роль ноды, а если её нет — роль, которую выбрал координатор.',
    details:
      'Приложение запускает воркера, задача стоит в «В работе». Если сюда вернули после отказа, замечания проверяющего ' +
      'или человека попадают в промпт воркера, и он стартует сразу — координатору делать ничего не нужно. ' +
      'Если задан показ, воркер сдаёт его вместе с done: описание и файлы из ветки (макеты, скриншоты). ' +
      'Человек видит показ на следующей ноде «Человек» — с превью картинок и кнопками «Открыть» / «В папке».',
    outcomes: { next: 'воркер выполнил orca-board done: дальше, обычно на проверку.' },
    fields: [
      'Роль — кто делает работу; задаёт роль задачи. Пусто — роль задачи от координатора.',
      'Что сделать на этапе — уходит воркеру в задание разделом «Этап», поверх текста задачи.',
      'Показать человеку — что воркер сдаёт на показ (например, варианты макета). Нужна нода «Человек» дальше по графу, иначе показ никто не увидит.',
      'Показ обязателен — done без показа отвергается, воркер получит подсказку.',
      'Колонка — задача на этом этапе всегда в «В работе», пока работает воркер.'
    ]
  },
  ask: {
    summary: 'Агент задаёт вопросы человеку, ответы попадают в промпт следующих этапов.',
    actor: 'Агент роли: роль ноды, а если её нет — роль задачи. Вопросы идут человеку, минуя координатора.',
    details:
      'Приложение запускает агента, он задаёт вопросы штатным orca-board ask; задача ждёт ответа в «Нужен ответ». ' +
      'Код на этапе агент не меняет. Когда всё выяснено, он сдаёт done — задача идёт дальше. Ответы получит следующая «Работа».',
    outcomes: { next: 'агент выполнил orca-board done: дальше, обычно в «Работу».' },
    fields: [
      'Роль — кто спрашивает; пусто — роль задачи. Роль этапа не становится ролью задачи.',
      'О чём спросить — обязательно: тема, что выяснить, сколько вопросов, в какой форме.'
    ]
  },
  gate: {
    summary: 'Агент-проверяющий смотрит ветку задачи и принимает или возвращает её.',
    actor: 'Агент выбранной роли (например, ревьюер) в отдельной задаче-проверке.',
    details:
      'Приложение создаёт задачу-проверку «<нода>: <задача>» на роль проверяющего и сразу запускает её воркера. ' +
      'Рабочая задача ждёт в колонке этапа. Решение — review accept или review reject с замечаниями; ' +
      'человек может решить за агента кнопками «Принять» / «Вернуть» у задачи.',
    outcomes: {
      accept: 'проверка пройдена: дальше, обычно мерж.',
      reject: 'возврат с замечаниями: обычно снова в «Работу»; замечания получит воркер.'
    },
    fields: [
      'Роль проверяющего — обязательна; служебные роли (координатор, ассистент) сюда не ставятся.',
      'Как проверять — критерии и команды, попадают в задание проверяющего.',
      'Колонка — где ждёт рабочая задача (по умолчанию «Ревью»).'
    ]
  },
  human: {
    summary: 'Решение за человеком: запрос в Инбоксе «Принять» / «Вернуть».',
    actor: 'Человек в приложении.',
    details:
      'Приложение создаёт запрос на одобрение в Инбоксе, задача ждёт в «Нужен ответ». В запросе — инструкции ноды, ' +
      'итог воркера, показ с «Работы» (если воркер его сдал) и ветка; после неудачного мержа — текст конфликта. ' +
      'В поле решения при «Принять» можно написать выбранный вариант — координатор его получит.',
    outcomes: {
      accept: 'человек принял: дальше (например, мерж или повтор мержа после разрешения конфликта).',
      reject: 'человек вернул с замечаниями: обычно в «Работу»; замечания получит воркер.'
    },
    fields: [
      'Что решить человеку — текст запроса: что посмотреть перед решением.',
      'Колонка — где ждёт задача (по умолчанию «Нужен ответ»).'
    ]
  },
  condition: {
    summary: 'Развилка: приложение проверяет условие и сразу ведёт задачу по «да» или «нет».',
    actor: 'Приложение — считает условие мгновенно, задача на этапе не стоит.',
    details:
      'Условия из закрытого списка: число заходов задачи в ноду (лимит повторов, например «после 3 отказов — к человеку») ' +
      'или роль рабочей задачи (например, проверка только для задач разработчика). Цикл из одних условий запрещён.',
    outcomes: {
      yes: 'условие выполнено.',
      no: 'условие не выполнено.'
    },
    fields: [
      'Условие «Число заходов в ноду» — нода и порог: «да», если задача заходила в неё не меньше N раз.',
      'Условие «Роль рабочей задачи» — «да», если роль задачи одна из отмеченных.'
    ]
  },
  merge: {
    summary: 'Приложение сливает ветку задачи в текущую ветку репозитория.',
    actor: 'Приложение — git merge --no-ff, без агента и человека.',
    details:
      'Незакоммиченные хвосты в worktree коммитятся, ветка сливается, worktree и ветка удаляются. ' +
      'При конфликте ветка и worktree остаются на месте. Ошибка не из-за конфликта останавливает задачу (workflow_blocked).',
    outcomes: {
      ok: 'ветка слита: дальше, обычно «Конец».',
      conflict: 'конфликт мержа, обычно к человеку: он разрешает конфликт в ветке и принимает (мерж повторится) или возвращает в работу.'
    },
    fields: ['Название — только для отображения.']
  },
  end: {
    summary: 'Задача закончена и уходит в «Готово».',
    actor: 'Приложение.',
    details:
      'Если ветка уже слита, задача просто закрывается. Если пришли без мержа, хвосты коммитятся, worktree убирается, ' +
      'а ветка остаётся: работа не попала в основную ветку, но и не потеряна.',
    outcomes: {},
    fields: [
      'Сюда приходят со слитой веткой — только для отображения на схеме, на поведение не влияет.',
      'Колонка — задача всегда уходит в колонку «Готово» (иначе прогон не закроется).'
    ]
  }
}
